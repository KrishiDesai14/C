#include<stdio.h>
int main () {

    char ch;
    char name[100];
    char i,n, c=0;

    printf("Enter name=>");
    gets(name);

    n=strln(name);
    {
        ch=name[i];
        printf("%c", ch);

    }
    return 0;
}